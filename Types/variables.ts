// function printName(person:{first:string, last:string}) :void{
//     console.log(person.first+" " + person.last);
// }

// printName({first:'Sadid', last:'369'})
type Point ={
    x:number;
    y:number;
}

// function doublePoint(point:Point):Point{
//     return point;
// }
// const points = doublePoint({x:2,y:3})
// console.log(points);
const user: string[] = ['']
const user1:Array<number>= [2];
const user2:Array<boolean>= [true];
const coord:Point[] =[];
coord.push({x:2,y:3})
console.log(coord);

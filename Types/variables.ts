function square(num:string="sadid"):string {
    return `hello ${num}`;
}

console.log(square());
const addnumber = ():number=>{return 5}

const colors = ['red', 'orange', 'yellow']

const upColor= colors.map(color=>{
 return   color.toUpperCase()
})

console.log(upColor);